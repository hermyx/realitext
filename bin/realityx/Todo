- Relationship (database ? or relation attribut in Person ?)
- In there, put "type of relation" (ex : friend, fiancee, etc.) and "trusting level"

- Implement a Game Attribute in Action

- Implement the EventGenerator
- Implement the TalkingEngine

- Add a Type type (human, object, animal, etc)
- Add a Clearance (with a Type type ?) to locations

- Add in the Word equals test something to do "if it's both, it's ok if it's masculin"

- Do the JUnit Tests
- Implement Exceptions